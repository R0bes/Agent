/**
 * Embedding Provider
 * 
 * Infrastructure layer for embedding provider
 */

import { embeddingClient } from "../../components/llm/embeddingClient";

export { embeddingClient as embeddingProvider };

