/**
 * Component Types
 * 
 * Re-exports types from legacy for backward compatibility
 * These types are still used by non-legacy components
 */

export type {
  Component,
  ServiceInterface as Service,
  ToolInterface as Tool,
  SourceInterface as Source,
  WorkerInterface as Worker,
  ToolContext,
  ToolResult,
  SourceMessage
} from "../legacy/components/types";


